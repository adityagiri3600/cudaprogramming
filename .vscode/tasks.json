{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build_cuda",
            "type": "shell",
            "command": "nvcc",
            "args": ["add.cu", "-o", "add_cuda"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}